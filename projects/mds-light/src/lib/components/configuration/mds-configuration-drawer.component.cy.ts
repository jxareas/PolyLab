import { MdsConfigurationDrawerComponent } from './mds-configuration-drawer.component';

describe('ConfigurationDrawerComponent', () => {
  it('should mount', () => {
    cy.mount(MdsConfigurationDrawerComponent);
  });
});
